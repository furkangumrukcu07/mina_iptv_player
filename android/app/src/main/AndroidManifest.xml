<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Push bildirim (Firebase Cloud Messaging): Android 13+ (API 33) runtime
         izni. Reddedilse bile uygulama normal çalışır; yalnızca bildirim
         gösterilmez. Hassas izin değildir, Play Store ek beyan gerektirmez. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Karekod ile playlist yükleme: cihazın yerel Wi-Fi/Ethernet IP'sini
         okumak ve LAN üzerinden HTTP sunucusu kurmak için. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- Ses Equalizer: BetterPlayer/ExoPlayer çıkışına bağlı AudioEffect.Equalizer
         için. Android 9+'da session=0 (global output mix) bazı cihazlarda
         kısıtlanır; bu durumda Dart tarafı kullanıcıya bilgi verir. -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- Play: READ_MEDIA_* kullanılmıyor; bağımlılık manifest birleşiminde gelirse düşürülür. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" tools:node="remove" />

    <!-- Phone + Android TV: leanback optional so the APK also installs on phones. -->
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <!-- PiP: Android’de ayrı bir uses-permission yok; etkinlikte supportsPictureInPicture yeterli.
         İsteğe bağlı özellik bildirimi (Play / uyumluluk). Kullanıcı bazen Ayarlar > Uygulamalar >
         Özel uygulama erişimi > Resim içinde resim ile uygulama bazında açmalıdır. -->
    <uses-feature
        android:name="android.software.picture_in_picture"
        android:required="false" />

    <!-- IPTV streams often use http://; without this, ExoPlayer fails on Android 9+. -->
    <!-- Telefon: @mipmap/ic_launcher (adaptif; roundIcon yok — TV / uyarlanabilir simge kılavuzu).
         Banner: @mipmap/tv_banner (yoğunluk merdiveni) + drawable-* tv_banner Play xhdpi kontrolü için. -->

    <!-- AppOps «attributionTag not declared» spam: Media3/Exo boş veya media tag kullanır.
         Doğru API <attribution> (manifest çocuğu); application meta-data değil. -->
    <attribution
        android:tag="@string/attribution_tag_empty"
        android:label="@string/attribution_label_media" />
    <attribution
        android:tag="@string/attribution_tag_media"
        android:label="@string/attribution_label_media" />

    <application
        android:label="Mina IPTV Player"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:banner="@mipmap/tv_banner"
        android:usesCleartextTraffic="true"
        android:extractNativeLibs="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:banner="@mipmap/tv_banner"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:screenOrientation="sensor"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:resizeableActivity="true"
            android:supportsPictureInPicture="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Impeller/Skia: [MinaRendererPolicy] + MainActivity Intent bayrağı (cihaza göre). -->

        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- FileProvider: kayıt / indirilen dosyaları paylaşmak için (Share
             intent → WhatsApp, Drive, Gallery, vb.). Yalnızca app-scoped
             external storage path'leri açığa çıkar. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
        <!-- Harici Oynatıcı: Android 11+ paket görünürlüğü için VLC, MX Player,
             Just Player, nPlayer vb. yüklü video oynatıcıları listeleyebilmek
             gerekiyor. Spesifik paket adları yerine MIME türüne göre query. -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="video/*" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="application/x-mpegURL" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
    </queries>
</manifest>
